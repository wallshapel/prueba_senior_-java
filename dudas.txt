Dudas durante la realización: El enunciado dice "Implementar en Java un servicio Rest que reciba como parámetros los atributos del objeto empleado (que será descrito más adelante) al ser invocado mediante el método GET"
lo que bien puede dar a entender que en empleado ya existe en la DB y va a ser consultado, obtenido... mediante GET que es lo normal. Pero más adelante menciona que el servicio SOAP lo va a guardar. Pero teniendo en cuenta que ya existe el empleado y lo obtenemos con GET por qué se va a guardar nuevamente?
De modo que consideré un error tipográfico el enunciado y en vez de GET opté por por POST, pero si se asumía este camino entonces el servicio SOAP ya no tendría nada que guardar porque el servicio REST ya lo habría hecho. Terminé optando por hacer un CRUD completo por separado tanto SOAP como API REST. ambos apuntando a la misma base de datos 